import{_ as d,o as s,c as a,a2 as i}from"./chunks/framework.Dj7FWXx_.js";const k=JSON.parse('{"title":"通知事件","description":"","frontmatter":{},"headers":[],"relativePath":"events/notice.md","filePath":"events/notice.md","lastUpdated":1789564099000}'),t={name:"events/notice.md"};function o(n,e,c,r,p,l){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="通知事件" tabindex="-1">通知事件 <a class="header-anchor" href="#通知事件" aria-label="Permalink to &quot;通知事件&quot;">​</a></h1><p>本文对应框架 v2.0.6。通知事件的 <code>category</code> 为 <code>notice</code>。群通知大类是 <code>group_notice</code>、认证权限为 <code>group_event</code>；好友通知大类是 <code>friend_notice</code>、认证权限为 <code>friend_event</code>。推荐使用精确监听器，由 SDK 声明对应权限。</p><h2 id="群成员变化" tabindex="-1">群成员变化 <a class="header-anchor" href="#群成员变化" aria-label="Permalink to &quot;群成员变化&quot;">​</a></h2><h3 id="group-member-joined" tabindex="-1">group_member_joined <a class="header-anchor" href="#group-member-joined" aria-label="Permalink to &quot;group_member_joined&quot;">​</a></h3><table tabindex="0"><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>group_id</code>, <code>group_name</code></td><td>目标群</td></tr><tr><td><code>user_id</code>, <code>nickname</code></td><td>已确认的新成员 QQ 与名称</td></tr><tr><td><code>user_uid</code></td><td>v2.0.6：原生成员 UID，即使 QQ 映射尚未准备好也能标识成员</td></tr><tr><td><code>invitor_id</code>, <code>invitor_nickname</code></td><td>邀请者，存在时返回</td></tr><tr><td><code>handler_id</code>, <code>handler_nickname</code></td><td>审核人，存在时返回</td></tr><tr><td><code>request_id</code>, <code>request_type</code></td><td>仅在确有审批记录时提供，不能用完成通知虚构申请标识</td></tr></tbody></table><p>框架直接处理已完成的原生入群通知，不强制依赖历史审批列表。新建群、直接邀请入群可能没有待审批记录；这不应阻止 <code>group_member_joined</code>。只有实际待处理申请才进入 <code>group_request_received</code> 链路。</p><p>群主和新成员接收的原生通知不同，框架分别解析；申请者通知中的处理者 UID 不会被误当作入群者。拒绝申请不生成加入事件，新申请使用新编号。申请接收、跨协议拒绝/批准、成员加入及恢复已通过双账号、四会话、跨两节点与双正向/单反向 WS 验证；Linux 主动申请入群是不支持的能力，邀请待审批不在本次实测范围内。</p><p>v2.0.6 修复群内成员身份查询：数字 QQ 通过目标群成员列表确认；UID 详情响应必须匹配请求群号和 UID，确认后才更新身份缓存。双账号、Android/Linux 的两条查询路径已实测一致，但不代表陌生好友 UID、所有通知的触发与投递已通过。无法确认数字 QQ 的事件仍保留原生 UID，不伪造数字身份。</p><h3 id="group-member-left" tabindex="-1">group_member_left <a class="header-anchor" href="#group-member-left" aria-label="Permalink to &quot;group_member_left&quot;">​</a></h3><table tabindex="0"><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>group_id</code>, <code>group_name</code></td><td>目标群</td></tr><tr><td><code>user_id</code>, <code>nickname</code></td><td>离群成员</td></tr><tr><td><code>operator_id</code>, <code>operator_nickname</code></td><td>操作者，主动退群时可能省略</td></tr><tr><td><code>request_id</code>, <code>request_type</code></td><td>有实际通知记录时才返回</td></tr><tr><td><code>user_uid</code>, <code>operator_uid</code></td><td>v2.0.6：已确认的原生成员/操作者 UID</td></tr><tr><td><code>reason</code></td><td>v2.0.6：<code>left</code>（主动退出）或 <code>kicked</code>（被移出）；原生通知未提供原因时省略</td></tr></tbody></table><p>已完成的原生退出推送与历史通知列表解耦；缺少历史记录或操作者信息时，不丢弃已确认的成员退出通知，也不填入猜测的 QQ 号。</p><p>主动退出、踢出、拒绝后重申请及批准恢复已完成双账号、Android/Linux、跨两节点和三 WS 的相应回归，包含退群账号自身通知。v2.0.6 同时修复推送顺序、旧登录会话取消和成员去重提交顺序。调用成功不等于所有相关账号必然收到通知；未知外部模板不因此视为通过。</p><p>自身通知可能只包含群号，框架据当前接收会话填写 <code>self_id</code>、<code>user_id</code> 和已知 UID；<strong>缺少原因和操作者时省略 <code>reason</code>、<code>operator_id</code> 等字段</strong>，不能默认成 <code>left</code> 或 <code>kicked</code>。框架为简略自身通知保留约一秒合并窗口，优先采用随后到达的完整原生通知，避免丢掉踢人原因与操作者；无完整通知时按简略数据发送。入群会先提交待发离群事件，离线或重登撤销旧会话任务。消费事件时先按成员身份更新群成员关系，只有存在明确原因时才展示“主动退出”或“被移除”。</p><h2 id="群管理变化" tabindex="-1">群管理变化 <a class="header-anchor" href="#群管理变化" aria-label="Permalink to &quot;群管理变化&quot;">​</a></h2><h3 id="结构化字段" tabindex="-1">结构化字段 <a class="header-anchor" href="#结构化字段" aria-label="Permalink to &quot;结构化字段&quot;">​</a></h3><p>以下为 v2.0.6 当前字段。群名、指定成员禁言/解除，以及框架 API 发起的名片、管理员和头衔变更已完成双账号、Android/Linux、跨节点、双正向/单反向 WS 的修改与恢复验证。服务器回读与原生推送区分来源，不根据设置接口成功伪造通知。其他客户端直接修改、未知模板、自然禁言到期和全员禁言不在该实测范围内。</p><table tabindex="0"><thead><tr><th>事件</th><th>原生字段</th></tr></thead><tbody><tr><td><code>group_admin_changed</code></td><td><code>group_id</code>、<code>target</code>、<code>body.set_admin</code>：设置 <code>true</code>，取消 <code>false</code></td></tr><tr><td><code>group_card_changed</code></td><td><code>group_id</code>、<code>target</code>、<code>body.card</code>：可为空字符串</td></tr><tr><td><code>group_name_changed</code></td><td><code>group_id</code>、<code>group_name</code>、<code>operator</code>、<code>body.group_name</code>；相同账号的连续重复群名通知短时去重</td></tr><tr><td><code>group_title_changed</code></td><td><code>group_id</code>、<code>target.user_id</code>、<code>body.title</code>，清空时为 <code>&quot;&quot;</code>；操作者仅在原生通知实际提供时返回</td></tr><tr><td><code>group_essence_changed</code></td><td><code>group_id</code>、<code>operator</code>、<code>target</code>（消息发送者）、<code>body.msg_seq</code>、<code>body.msg_random</code>、<code>body.operation</code>（<code>add</code> / <code>remove</code>）、<code>body.time</code>（原生秒级时间）</td></tr><tr><td><code>message_reaction_changed</code></td><td><code>group_id</code>、<code>operator</code>、<code>body.msg_seq</code>、<code>body.emoji_id</code>、<code>body.count</code>、<code>body.operation</code>（<code>add</code> / <code>remove</code>）</td></tr></tbody></table><p>表情回应的 <code>body.message_id</code> 仅在当前账号的消息引用缓存命中时返回；取消后 <code>count</code> 可为 <code>0</code>。<code>operator</code> / <code>target</code> 可提供 <code>uid</code>，只有确认 QQ 号时才提供 <code>user_id</code>。原生管理员通知不含操作者时，不虚构 <code>operator</code>。这些结构化通知不保证包含 <code>body.summary</code>。</p><p>同一账号事件投递到不同 WS 时共享 <code>event_id</code>；两个账号收到同一群操作是两条账号事件，以 <code>self_id + client_type</code> 区分。</p><p>头衔的结构化分支按原生通知类型及完整 JSON 内容解析，不从“头衔”字样猜测新值；不完整、未知类型或截断通知不生成这类结构化事件。JCE 和 MsgPush 双通道按账号/协议、群和目标成员对连续相同值短时去重，不吞掉修改后恢复原值的变化。设置 API 成功、成员列表可回读新头衔，不代表服务端必然向该会话推送通知，因此不能替代事件验收。</p><h3 id="服务器回读确认" tabindex="-1">服务器回读确认 <a class="header-anchor" href="#服务器回读确认" aria-label="Permalink to &quot;服务器回读确认&quot;">​</a></h3><p>框架执行名片、管理员和头衔设置时，先读取真实旧值，再由在线账号各自回读群成员列表。只有确认读取账号和目标均属于该群、实际值发生改变，才补发相应事件；不根据设置 API 的成功响应生成事件，也不自动重试设置操作。回读事件额外提供顶层 <code>source: &quot;server_readback&quot;</code> 和 <code>confirmed_at</code>（确认时间，毫秒），不包含无法确认的 <code>operator</code>。原生推送省略这两个字段；<code>occurred_at</code> 仍是框架生成事件的时间，不是服务器提供的历史操作时间。</p><p>原生推送和回读按账号/协议、群、目标及属性共同去重，连续相同值一分钟内不重复；A → B → A 不会被吞掉。账号离线、重登后，旧登录的回读结果不再生效。读取失败不会补发推测事件。该机制目前由框架设置 API 触发，不是覆盖所有群的周期轮询，不保证其他客户端的每次中间变化均被观察到。</p><h3 id="系统提示结构" tabindex="-1">系统提示结构 <a class="header-anchor" href="#系统提示结构" aria-label="Permalink to &quot;系统提示结构&quot;">​</a></h3><p><code>group_system_tip</code> 使用下列摘要结构。专用群管理事件由原生结构化字段或明确标注的服务器回读生成，不能仅凭系统卡片文案生成；其字段以上表为准。事件名存在不代表所有协议入口都已完成实机验收。</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  group_id,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  group_name,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  operator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { user_id, nickname }, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 能确认时出现</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  target</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { user_id, nickname },   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 能确认时出现</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  body</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    summary</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;QQ 客户端可见的系统提示&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    participants</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ user_id, nickname }]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>框架从 QQ 原生系统卡片中提取可读摘要和已确认的参与人，不把易变化的 XML 当作公共参数。某个字段无法从本次推送确认时会省略，不会填入 <code>0</code> 或猜测内容。</p><p>v2.0.6 修复删除按“管理员”“群名片”“群名”“头衔”“精华”“上传文件”“戳”等文字猜测专用类型的逻辑。仅有摘要的系统卡片统一发送为 <code>group_system_tip</code>，不会产生缺少 <code>body.card</code>、<code>body.title</code>、<code>body.file_id</code> 等必要字段的专用事件。完整原生通知仍优先走专用解析；已识别的禁言、入群申请伴随卡片不重复生成事件。</p><p>已验证的表情回应伴随提示只发给消息作者的在线协议，不含无法确认的操作者；回应事件按原生结构提供序号、表情与添加/取消状态。该真实模板的验证不代表其他客户端任意卡片都能解析。</p><h2 id="群文件上传-v2-0-6" tabindex="-1">群文件上传（v2.0.6） <a class="header-anchor" href="#群文件上传-v2-0-6" aria-label="Permalink to &quot;群文件上传（v2.0.6）&quot;">​</a></h2><p>目录读取对有歧义的末页扩大窗口并校验前缀，满 100 条时采用重叠窗口确认尾部；视图不一致或无法确认完整性时返回错误。总条目上限 10000，整次读取预算 45 秒。99/100/101 条边界及混合文件/目录已交叉验证，不承诺无限目录或持续并发修改仍能获得一致快照。详见<a href="/api/get_group_files_by_folder.html#分页与完整性">目录分页与完整性</a>。</p><p><code>group_file_uploaded</code> 的 <code>group_id</code> 表示目标群，<code>operator.user_id</code> 是经服务端文件列表确认的上传者。完整文件消息包含 <code>body.file_id</code>、<code>file_name</code>、<code>file_size</code>、<code>busid</code>，以及消息本身提供的字段。</p><p>部分会话只收到原生目录变更提示。框架在收到提示后回读真实文件列表及子目录，确认本次账号在线期间新出现的上传，才产生事件；不会根据上传 API 成功响应伪造通知。此路径额外提供 <code>body.upload_time</code>（秒）和 <code>body.parent_folder_id</code>，不提供无法确认的 <code>msg_seq</code>。同一账号/协议运行期按群和文件 ID 去重，重复提示不重复投递；回读失败或列表不完整不会当作成功处理，也不会重播上线之前的历史文件。</p><p>v2.0.6在文件动态提交后，主动唤醒上传者当时已在线的各协议会话，由它们独立回读服务器目录；其他会话确认新文件后，也可以唤醒对应上传者。上传响应不会直接变成事件，回读仍须校验完整服务器数据。该路径标记顶层 <code>source: &quot;server_readback&quot;</code> 和毫秒级 <code>confirmed_at</code>。上传提交未进入动态提交阶段时不启动此确认；动态响应丢失也只回读，不重试上传。提交前捕获登录代次，旧上传完成不能唤醒重登后的会话。</p><p>已验证上传者与接收者、接收方离线时上传者独立确认、重登不重放，以及双账号、双协议、两节点、三 WS 的交叉上传和反向重连。未知完整文件通知模板、外部无目录提示的上传和长期高负载未包含在该实测范围内。</p><p>目录回读任务绑定到账号的本次登录：离线或重登取消旧任务，迟到响应不得继续重试、修改新登录的去重记录或提交旧事件。已发出的底层请求可能仍等待响应或超时，但结果不会跨登录生效。离线期间上传的文件可从目录 API 查询，不会在重登后当成新上传重复广播。</p><p>事件目录回读与群属性回读共享最多 8 个在途查询名额，等待名额也计入超时。一次文件树确认（含重试）总预算为 45 秒；一次群属性变更的所有观察账号共用 15 秒确认预算。超时会记录回读失败，不虚构变化；这不是全部账号必达的保证，也不限制插件主动调用的普通目录查询。</p><h2 id="群禁言" tabindex="-1">群禁言 <a class="header-anchor" href="#群禁言" aria-label="Permalink to &quot;群禁言&quot;">​</a></h2><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">api.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;group_member_muted&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">event</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(event.target.user_id, event.body.duration)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  group_id,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  group_name,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  operator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { user_id, nickname },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  target</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { user_id, nickname },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  body</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    duration</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">600</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>duration</code> 单位为秒，<code>0</code> 表示解除禁言；<code>status</code> 表示事件发生后的禁言状态。</p><h2 id="消息撤回" tabindex="-1">消息撤回 <a class="header-anchor" href="#消息撤回" aria-label="Permalink to &quot;消息撤回&quot;">​</a></h2><h3 id="group-message-recalled" tabindex="-1">group_message_recalled <a class="header-anchor" href="#group-message-recalled" aria-label="Permalink to &quot;group_message_recalled&quot;">​</a></h3><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  group_id,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  group_name,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  operator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { user_id, nickname },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  target</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { user_id, nickname },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  body</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    msg_seq,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    msg_time,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    msg_random,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    prompt_text,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    original</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>original</code> 仅在框架缓存仍保存该消息时出现，包含原发送者、结构化消息段和文本摘要。</p><h3 id="friend-message-recalled" tabindex="-1">friend_message_recalled <a class="header-anchor" href="#friend-message-recalled" aria-label="Permalink to &quot;friend_message_recalled&quot;">​</a></h3><p>好友撤回事件包含 <code>operator</code> 和 <code>body</code>；<code>body</code> 中返回本次原生推送能够确认的消息标识。好友消息未进入框架缓存或 QQ 未返回原消息时不生成虚假的 <code>original</code>。</p><p>好友撤回包含以下原生消息标识；<code>msg_seq</code> 和 <code>client_seq</code> 均不是可直接传给 <code>delete_msg</code> 的公共 <code>message_id</code>。</p><table tabindex="0"><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><code>body.msg_seq</code></td><td>服务端消息序号，对应发送响应中的 <code>msg_seq</code></td></tr><tr><td><code>body.client_seq</code></td><td>客户端序号，对应私聊原生消息回推的 <code>msg.seq</code></td></tr><tr><td><code>body.msg_random</code></td><td>消息随机标识，与客户端序号共同关联原消息</td></tr><tr><td><code>body.from_uid</code> / <code>body.to_uid</code></td><td>原生确认的发送方与接收方 UID</td></tr><tr><td><code>body.prompt_text</code></td><td>存在时返回的撤回提示文案，不能视为服务端错误</td></tr></tbody></table><p>同一 QQ 的不同协议会话可能分别收到通知。插件应结合 <code>self_id</code>、<code>client_type</code>、双方 UID 和消息标识处理，不能只用一个序号跨账号匹配。API 成功响应和原生撤回事件是两个独立结果；框架不会据 API 响应伪造事件。</p><h2 id="好友关系与互动" tabindex="-1">好友关系与互动 <a class="header-anchor" href="#好友关系与互动" aria-label="Permalink to &quot;好友关系与互动&quot;">​</a></h2><h3 id="friend-added" tabindex="-1">friend_added <a class="header-anchor" href="#friend-added" aria-label="Permalink to &quot;friend_added&quot;">​</a></h3><p>v2.0.6在收到服务器原生好友关系确认通知后发送，<code>operator</code> 是新好友（含可确认的 <code>user_id</code>、<code>uid</code>、<code>nickname</code>）。<code>body.source</code> 固定为 <code>native_push</code>，<code>body.time</code> 是服务器确认时间（Unix 秒）。同一账号、协议重复收到同一通知时短时去重；不同协议、账号各自投递，不互相吞掉。</p><p>不再因“同意申请”API 返回成功直接生成此事件。<code>friend_added</code> 不包含申请 <code>flag</code>；需要处理申请时，应使用 <code>friend_request_received.flag</code>。API 成功、好友列表回读和事件到达是三个独立验证项。</p><h3 id="profile-liked" tabindex="-1">profile_liked <a class="header-anchor" href="#profile-liked" aria-label="Permalink to &quot;profile_liked&quot;">​</a></h3><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  operator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { user_id, nickname },</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  body</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;赞了我的资料卡1次&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">count_source</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;native_text&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>v2.0.6用原生通知类型识别资料点赞，不依赖提示里是否出现“戳”或“正在输入”。修复了把通知内部固定值 <code>2</code> 当作点赞次数的问题。</p><p><code>text</code> 保留服务器原文。当前原生通知的数量来自已验证的“赞了我的资料卡N次”模板，所以附带 <code>count_source: &#39;native_text&#39;</code>；<code>count</code> 表示本次次数，不是累计获赞。遇到未识别模板或无效数量时不返回 <code>count</code> 和 <code>count_source</code>，插件应显示原文，不应把字段缺失视为 0 次或固定 2 次。此修复由 v2.0.6 提供。</p><h3 id="typing-status-changed" tabindex="-1">typing_status_changed <a class="header-anchor" href="#typing-status-changed" aria-label="Permalink to &quot;typing_status_changed&quot;">​</a></h3><p><code>operator</code> 表示状态来源好友。原生输入状态通知提供 <code>body.state</code>（保留原始状态码）、<code>body.from_uid</code>、<code>body.to_uid</code>，以及存在时的 <code>body.text</code>。已验证 <code>state=1</code> 开始、<code>state=0</code> 停止；不要把所有非零状态都当作正在输入，也不要依靠文本判断状态。</p><p>开始/停止状态已通过两个账号跨节点、双正向 WS 实测。Linux 发起、接收方 Android/Linux 同时在线的交叉测试也已通过：状态只描述发起者，双 WS 的事件 ID 与内容一致。不将发送接口成功当作接收事件的证据。</p><h3 id="user-poked" tabindex="-1">user_poked <a class="header-anchor" href="#user-poked" aria-label="Permalink to &quot;user_poked&quot;">​</a></h3><p>在群聊中会同时包含 <code>group_id</code>、<code>operator</code> 和 <code>target</code>；好友场景至少包含已确认的发起者。无法确认目标时不补猜测账号。</p><p>框架按原生互动模板解析群聊与私聊戳一戳，提供已确认的 <code>operator</code>、<code>target</code> 和 <code>body.action</code>、<code>body.suffix</code>。<code>action</code> 可以是“拍了拍”、自定义文案或其他语言，插件不应通过其中是否包含“戳”字判定事件。私聊没有 <code>group_id</code>，使用 <code>friend_event</code> 权限；群内使用 <code>group_event</code>。</p><p>群内和私聊戳一戳已通过两个 Linux 测试账号相互发起、双方 Android/Linux 会话接收、双正向 WS 投递的交叉验证，逐条核对操作者和目标账号。</p><p>精华事件的 <code>msg_seq</code>、<code>msg_random</code> 是原生标识，不是公共 <code>message_id</code>。需要调用精华操作时，请从当前账号收到、发送或回读的群消息获取公共引用。v2.0.6修复旧查询返回 <code>12002</code>、Linux 在线却因缺少 Android SKey 无法查询的问题。两个 QQ 的 Android/Linux 会话已完成设置、查询、取消和再次查询验证；原生推送的操作类型、消息序号、随机标识、操作者及双 WS 一致性分别校验通过。其他未覆盖事件不据此宣称通过。</p>`,66)])])}const g=d(t,[["render",o]]);export{k as __pageData,g as default};
